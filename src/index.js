'use strict';

/**
 * pii-guard main entry point.
 *
 * As a library:  const { PiiGuard } = require('pii-guard');
 * As a proxy:    node src/proxy/server.js  (or: npx pii-guard)
 */

module.exports = require('./sdk/index');
