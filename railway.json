{
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "docker/Dockerfile"
  },
  "deploy": {
    "startCommand": "node src/proxy/server.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 15,
    "restartPolicyType": "always"
  }
}
